import json

# Load Google Drive credentials from JSON file
with open('your_credentials_file.json', 'r') as f:
    credentials = json.load(f)

# Access the individual fields
client_id = credentials['web']['525216497500-np3i3kdpj749jbov4ojqsc8ds4lm3qj9.apps.googleusercontent.com']
project_id = credentials['web']['rapberry-pi-camera']
auth_uri = credentials['web']["https://accounts.google.com/o/oauth2/auth"]
token_uri = credentials['web']["https://oauth2.googleapis.com/token"]

# auth_provider_x509_cert_url = credentials['web']['auth_provider_x509_cert_url']

auth_provider_x509_cert_url = credentials['web']["https://www.googleapis.com/oauth2/v1/certs","client_secret"]
client_secret = credentials['web']['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']
redirect_uris = credentials['web']["http://localhost:3000/auth-api/callback"]

# Now you can use these variables as needed in your script
